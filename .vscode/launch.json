{
    "version": "0.2.0",
    "configurations": [
		{
			"name": "(ctest) Launch",
			"type": "lldb",
			"cwd": "${cmake.testWorkingDirectory}",
			"request": "launch",
			"program": "${cmake.testProgram}",
			"args": [ "${cmake.testArgs}" ],
			// other options...
		}
    ],
	
}